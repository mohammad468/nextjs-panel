import type { NextPage } from "next";
import { Container } from "react-bootstrap";

const Home: NextPage = () => {
  return (
    <Container fluid>
      <h1>homePage</h1>
    </Container>
  );
};

export default Home;
